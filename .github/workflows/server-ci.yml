name: Server CI
run-name: ${{github.ref}} Server

on: workflow_dispatch

jobs:
  build:
    runs-on: [self-hosted, build]
    steps:
      - name: Clone repo
        uses: actions/checkout@v6
        with:
          path: application
      - name: Docker build
        run: docker buildx build -t virabshetkar/todo-service:${{github.sha}} .
        working-directory: application/server
      - name: Docker push
        run: docker push virabshetkar/todo-service:${{github.sha}}
